"use client";

import { motion, useInView } from "framer-motion";
import { useRef, useState } from "react";

export default function FamilySection() {
  return null; // Family section removed per client request
}
